# ==================== AI API 配置 ====================
# 从 Moonshot AI 或其他兼容 OpenAI API 的服务获取
API_KEY=your_api_key_here
BASE_URL=https://api.moonshot.cn/v1
MODEL=kimi-k2-turbo-preview

# ==================== 数据库配置 ====================
# PostgreSQL 数据库连接地址
# 格式：postgresql://用户名:密码@主机:端口/数据库名

# 本地调试（连接 Docker 中的 PostgreSQL）
# Docker Compose 启动后，PostgreSQL 映射到 localhost:5433（避免与本地 PostgreSQL 冲突）
DATABASE_URL=postgresql://newstudy:newstudy123@localhost:5433/newstudy_db

# 如果在 Docker 容器内运行后端服务，使用服务名连接
# DATABASE_URL=postgresql://newstudy:newstudy123@postgres:5432/newstudy_db

# 如果使用本地 PostgreSQL（非 Docker），修改为你的连接信息
# DATABASE_URL=postgresql://your_username:your_password@localhost:5432/your_database
