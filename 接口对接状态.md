# 接口对接状态

## ✅ 后端已实现的接口

1. ✅ POST /notes - 创建笔记
2. ✅ GET /notes/{note_id} - 获取笔记详情
3. ✅ POST /notes/{note_id}/flash-cards/generate - 生成闪词卡片
4. ✅ GET /notes/{note_id}/flash-cards/progress - 获取闪词学习进度

## ❌ 前端还未对接的接口

### 1. 创建笔记接口 (create_note_controller.dart:199)
- ❌ TODO: 调用后端API保存笔记
- ❌ 缺少 API 路径配置
- ❌ 缺少 HTTP 服务方法
- ❌ 缺少响应模型

### 2. 获取笔记详情接口 (note_detail_controller.dart:21)
- ❌ TODO: 从后端或本地数据库加载笔记
- ❌ 缺少 API 路径配置
- ❌ 缺少 HTTP 服务方法
- ✅ 已有 NoteModel 模型

### 3. 生成闪词卡片接口 (note_detail_controller.dart:74)
- ❌ TODO: 调用后端AI接口生成闪词
- ❌ 缺少 API 路径配置
- ❌ 缺少 HTTP 服务方法
- ❌ 缺少响应模型

### 4. 获取闪词学习进度接口 (note_detail_controller.dart:50)
- ❌ 使用模拟数据
- ❌ 缺少 API 路径配置
- ❌ 缺少 HTTP 服务方法
- ✅ 已有 FlashCardProgress 模型

## 📋 需要完成的工作

1. **在 api_config.dart 中添加 API 路径**
   - createNote
   - getNote
   - generateFlashCards
   - getFlashCardProgress

2. **在 note_models.dart 中添加响应模型**
   - NoteResponse (用于创建和获取笔记)
   - FlashCardGenerateResponse (用于生成闪词)
   - FlashCardProgressResponse (已有模型，需要添加 fromJson)

3. **在 http_service.dart 中添加 API 方法**
   - createNote()
   - getNote()
   - generateFlashCards()
   - getFlashCardProgress()

4. **在控制器中调用 API**
   - create_note_controller.dart 中调用 createNote()
   - note_detail_controller.dart 中调用 getNote(), generateFlashCards(), getFlashCardProgress()

5. **处理路由参数传递**
   - 创建笔记后需要传递 note_id 到详情页
   - 详情页需要从路由参数获取 note_id
