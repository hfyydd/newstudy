# 强制登录实现规划

## 一、登录策略

### 1.1 强制登录策略
- ✅ **APP 启动时检查登录状态**
- ✅ **未登录用户强制跳转到登录页**
- ✅ **登录后才能使用所有功能**
- ✅ **登录状态持久化（使用 Refresh Token）**

### 1.2 登录方式
1. **Google Sign-In**（除中国外）
2. **Apple Sign-In**（iOS/macOS 平台）
3. **邮箱验证码登录**（全球通用）

## 二、架构设计

### 2.1 认证流程

```
APP 启动
    ↓
检查本地是否有 Refresh Token
    ↓
┌─────────────────────┐
│  有 Refresh Token?   │
└─────────────────────┘
    ↓ 是              ↓ 否
验证 Token 有效性    跳转登录页
    ↓
┌─────────────────────┐
│  Token 有效?        │
└─────────────────────┘
    ↓ 是              ↓ 否
进入主页面          跳转登录页
```

### 2.2 路由守卫机制

```
用户访问任何页面
    ↓
检查认证状态
    ↓
┌─────────────────────┐
│  已登录?            │
└─────────────────────┘
    ↓ 是              ↓ 否
允许访问            跳转登录页
```

## 三、文件结构

### 3.1 新增文件

```
lib/
├── pages/
│   └── auth/
│       ├── login_page.dart          # 登录页面
│       ├── login_controller.dart    # 登录控制器
│       └── login_state.dart         # 登录状态
├── services/
│   └── auth_service.dart            # 认证服务（Token管理、API调用）
├── config/
│   └── auth_controller.dart         # 全局认证状态控制器
└── utils/
    └── region_detector.dart          # 区域检测工具（判断是否中国）
```

### 3.2 修改文件

```
lib/
├── app/
│   └── app.dart                     # 添加 AuthController 初始化
├── routes/
│   ├── app_routes.dart              # 添加登录路由
│   └── app_pages.dart               # 添加登录页面路由配置
└── services/
    └── http_service.dart             # 添加 Token 拦截器
```

## 四、核心组件设计

### 4.1 AuthController（全局认证状态管理）

**职责**：
- 管理登录状态
- 管理 Token（Access Token、Refresh Token）
- 管理当前用户信息
- 提供登录/登出方法
- 提供 Token 刷新方法
- 启动时检查登录状态

**状态变量**：
```dart
class AuthController extends GetxController {
  // 登录状态
  final isAuthenticated = false.obs;
  
  // 当前用户信息
  final currentUser = Rxn<User>();
  
  // Token
  final accessToken = Rxn<String>();
  final refreshToken = Rxn<String>();
  
  // 加载状态
  final isLoading = false.obs;
}
```

**核心方法**：
- `checkAuthStatus()`: 启动时检查登录状态
- `loginWithGoogle()`: Google 登录
- `loginWithApple()`: Apple 登录
- `loginWithEmail()`: 邮箱验证码登录
- `sendEmailCode()`: 发送邮箱验证码
- `logout()`: 登出
- `refreshAccessToken()`: 刷新 Access Token
- `clearAuth()`: 清除认证信息
```

### 4.2 AuthService（认证服务）

**职责**：
- Token 存储和读取（使用 flutter_secure_storage）
- 调用后端认证 API
- Token 验证

**核心方法**：
- `saveTokens()`: 保存 Token
- `getTokens()`: 读取 Token
- `clearTokens()`: 清除 Token
- `verifyToken()`: 验证 Token 有效性
- `callAuthAPI()`: 调用认证相关 API

### 4.3 LoginPage（登录页面）

**UI 布局**：
```
┌─────────────────────────┐
│      FlashMind          │
│    (Logo/App Name)      │
│                         │
│   [Google 登录按钮]      │  ← 中国区域隐藏
│   [Apple 登录按钮]       │  ← 仅 iOS/macOS
│                         │
│   ─────── 或 ───────    │
│                         │
│   邮箱登录               │
│   [邮箱输入框]           │
│   [发送验证码按钮]       │
│   [验证码输入框]         │
│   [登录按钮]             │
│                         │
│   隐私政策 | 服务条款    │
└─────────────────────────┘
```

**功能**：
- 根据平台和区域动态显示登录方式
- 处理各种登录方式的交互
- 显示加载状态和错误提示
- 登录成功后跳转到主页

### 4.4 RegionDetector（区域检测工具）

**功能**：
- 检测用户所在区域（判断是否中国）
- 决定显示/隐藏 Google Sign-In
- 决定显示/隐藏微信登录（如果实现）

**核心方法**：
- `shouldHideGoogleSignIn`: 是否隐藏 Google 登录
- `shouldShowWeChatLogin`: 是否显示微信登录
- `getCurrentRegionCode()`: 获取当前区域代码

## 五、路由设计

### 5.1 路由配置

```dart
// app_routes.dart
class AppRoutes {
  static const login = '/login';      // 新增
  static const main = '/main';
  // ... 其他路由
}

// app_pages.dart
static const initial = AppRoutes.login;  // 修改：初始路由改为登录页

static final routes = [
  GetPage(
    name: AppRoutes.login,
    page: () => const LoginPage(),
    binding: BindingsBuilder(() {
      Get.lazyPut<LoginController>(() => LoginController());
    }),
  ),
  // ... 其他路由
];
```

### 5.2 路由守卫

**实现方式**：
1. **中间件方式**：在 `GetMaterialApp` 中使用 `routingCallback`
2. **页面级检查**：在每个需要认证的页面 `onInit` 中检查

**推荐方案**：使用 GetX 的 `routingCallback`

```dart
GetMaterialApp(
  routingCallback: (routing) {
    // 检查是否需要认证
    if (_needsAuth(routing.current) && !AuthController.to.isAuthenticated.value) {
      Get.offAllNamed(AppRoutes.login);
    }
  },
)
```

## 六、Token 管理

### 6.1 Token 存储

**使用 `flutter_secure_storage`**：
- Access Token：存储在内存或安全存储（短期）
- Refresh Token：存储在安全存储（长期）

**存储键名**：
- `access_token`
- `refresh_token`
- `token_expires_at`

### 6.2 HTTP 拦截器

**自动添加 Token**：
```dart
_dio.interceptors.add(
  InterceptorsWrapper(
    onRequest: (options, handler) {
      final token = AuthController.to.accessToken.value;
      if (token != null) {
        options.headers['Authorization'] = 'Bearer $token';
      }
      handler.next(options);
    },
    onError: (error, handler) {
      // Token 过期，自动刷新
      if (error.response?.statusCode == 401) {
        AuthController.to.refreshAccessToken().then((success) {
          if (success) {
            // 重试原请求
          } else {
            // 刷新失败，跳转登录页
            Get.offAllNamed(AppRoutes.login);
          }
        });
      }
      handler.next(error);
    },
  ),
);
```

### 6.3 Token 刷新机制

**自动刷新流程**：
1. Access Token 过期（401 错误）
2. 使用 Refresh Token 请求新 Token
3. 更新 Access Token
4. 重试原请求
5. 如果 Refresh Token 也过期，跳转登录页

## 七、登录流程设计

### 7.1 Google 登录流程

```
用户点击 Google 登录
    ↓
调用 google_sign_in 插件
    ↓
获取 Google ID Token
    ↓
发送到后端验证
    ↓
后端返回 JWT Token + 用户信息
    ↓
保存 Token 到本地
    ↓
更新 AuthController 状态
    ↓
跳转到主页面
```

### 7.2 Apple 登录流程

```
用户点击 Apple 登录
    ↓
检查平台（iOS/macOS）
    ↓
调用 sign_in_with_apple 插件
    ↓
获取 Apple ID Token
    ↓
发送到后端验证
    ↓
后端返回 JWT Token + 用户信息
    ↓
保存 Token 到本地
    ↓
更新 AuthController 状态
    ↓
跳转到主页面
```

### 7.3 邮箱验证码登录流程

```
用户输入邮箱
    ↓
点击发送验证码
    ↓
后端发送验证码到邮箱
    ↓
用户输入验证码
    ↓
点击登录
    ↓
后端验证验证码
    ↓
后端返回 JWT Token + 用户信息
    ↓
保存 Token 到本地
    ↓
更新 AuthController 状态
    ↓
跳转到主页面
```

## 八、启动流程

### 8.1 APP 启动流程

```
main() 函数
    ↓
初始化 GetX
    ↓
初始化 AuthController
    ↓
AuthController.onInit()
    ↓
检查本地是否有 Refresh Token
    ↓
┌─────────────────────┐
│  有 Refresh Token?  │
└─────────────────────┘
    ↓ 是              ↓ 否
验证 Token 有效性    设置 isAuthenticated = false
    ↓
┌─────────────────────┐
│  Token 有效?        │
└─────────────────────┘
    ↓ 是              ↓ 否
设置 isAuthenticated = true
获取用户信息         清除 Token
    ↓                设置 isAuthenticated = false
    ↓
GetMaterialApp
    ↓
检查 initialRoute
    ↓
┌─────────────────────┐
│  已登录?            │
└─────────────────────┘
    ↓ 是              ↓ 否
跳转主页面           跳转登录页
```

## 九、依赖包

### 9.1 需要添加的依赖

```yaml
dependencies:
  # Google 登录
  google_sign_in: ^6.2.1
  
  # Apple 登录（iOS/macOS）
  sign_in_with_apple: ^6.1.0
  
  # Token 安全存储
  flutter_secure_storage: ^9.0.0
  
  # 平台检测（已有）
  # dart:io 已包含 Platform
  
  # HTTP 请求（已有）
  dio: ^5.4.0
  
  # 状态管理（已有）
  get: ^4.6.6
```

## 十、后端 API 设计

### 10.1 登录相关 API

#### 1. Google 登录
```
POST /auth/google/login
Request: { "id_token": "..." }
Response: { "access_token": "...", "refresh_token": "...", "user": {...} }
```

#### 2. Apple 登录
```
POST /auth/apple/login
Request: { "id_token": "...", "authorization_code": "..." }
Response: { "access_token": "...", "refresh_token": "...", "user": {...} }
```

#### 3. 发送邮箱验证码
```
POST /auth/email/send-code
Request: { "email": "user@example.com" }
Response: { "success": true, "expires_in": 300 }
```

#### 4. 邮箱验证码登录
```
POST /auth/email/verify-code
Request: { "email": "user@example.com", "code": "123456" }
Response: { "access_token": "...", "refresh_token": "...", "user": {...} }
```

#### 5. 刷新 Token
```
POST /auth/refresh
Request: { "refresh_token": "..." }
Response: { "access_token": "...", "refresh_token": "..." }
```

#### 6. 验证 Token（可选）
```
GET /auth/verify
Headers: { "Authorization": "Bearer {access_token}" }
Response: { "valid": true, "user": {...} }
```

#### 7. 登出
```
POST /auth/logout
Headers: { "Authorization": "Bearer {access_token}" }
Response: { "success": true }
```

## 十一、实施步骤

### Phase 1: 基础架构 ✅
1. ✅ 创建 AuthController
2. ✅ 创建 AuthService
3. ✅ 创建 RegionDetector
4. ✅ 配置 Token 存储
5. ✅ 配置 HTTP 拦截器

### Phase 2: 登录页面 ✅
1. ✅ 创建 LoginPage UI
2. ✅ 创建 LoginController
3. ✅ 实现区域检测逻辑
4. ✅ 实现平台检测逻辑

### Phase 3: 登录集成 ✅
1. ✅ 集成 Google 登录
2. ✅ 集成 Apple 登录
3. ✅ 实现邮箱验证码登录
4. ✅ 实现登录状态管理

### Phase 4: 路由和守卫 ✅
1. ✅ 修改初始路由为登录页
2. ✅ 实现路由守卫
3. ✅ 实现启动时认证检查
4. ✅ 实现 Token 自动刷新

### Phase 5: 集成测试 ✅
1. ✅ 测试各种登录方式
2. ✅ 测试 Token 刷新
3. ✅ 测试路由守卫
4. ✅ 测试登出功能
5. ✅ 测试多语言支持

## 十二、关键实现点

### 12.1 启动时认证检查

**在 AuthController.onInit() 中**：
```dart
@override
void onInit() {
  super.onInit();
  checkAuthStatus();
}

Future<void> checkAuthStatus() async {
  isLoading.value = true;
  try {
    // 1. 检查本地是否有 Refresh Token
    final refreshToken = await AuthService.getRefreshToken();
    if (refreshToken == null) {
      isAuthenticated.value = false;
      return;
    }
    
    // 2. 验证 Refresh Token 是否有效
    final isValid = await AuthService.verifyToken(refreshToken);
    if (!isValid) {
      await AuthService.clearTokens();
      isAuthenticated.value = false;
      return;
    }
    
    // 3. 获取用户信息
    await loadUserInfo();
    isAuthenticated.value = true;
  } catch (e) {
    await AuthService.clearTokens();
    isAuthenticated.value = false;
  } finally {
    isLoading.value = false;
  }
}
```

### 12.2 登录页面显示逻辑

```dart
Widget build(BuildContext context) {
  return Column(
    children: [
      // Google 登录（中国区域隐藏）
      if (!RegionDetector.shouldHideGoogleSignIn)
        GoogleSignInButton(),
      
      // Apple 登录（仅 iOS/macOS）
      if (Platform.isIOS || Platform.isMacOS)
        AppleSignInButton(),
      
      // 邮箱登录（始终显示）
      EmailLoginSection(),
    ],
  );
}
```

### 12.3 路由守卫实现

**方案1：使用 routingCallback（推荐）**
```dart
GetMaterialApp(
  routingCallback: (routing) {
    final needsAuth = _needsAuthentication(routing.current);
    final isAuth = AuthController.to.isAuthenticated.value;
    
    if (needsAuth && !isAuth) {
      Get.offAllNamed(AppRoutes.login);
    }
  },
)

bool _needsAuthentication(String route) {
  // 登录页不需要认证
  if (route == AppRoutes.login) return false;
  // 其他页面都需要认证
  return true;
}
```

**方案2：在页面中检查**
```dart
class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 检查登录状态
    if (!AuthController.to.isAuthenticated.value) {
      WidgetsBinding.instance.addPostFrameCallback((_) {
        Get.offAllNamed(AppRoutes.login);
      });
      return const SizedBox.shrink();
    }
    // ... 正常页面内容
  }
}
```

## 十三、错误处理

### 13.1 登录失败处理
- 显示友好的错误提示
- 支持重试
- 记录错误日志

### 13.2 Token 过期处理
- 自动刷新 Token
- 刷新失败时跳转登录页
- 提示用户重新登录

### 13.3 网络错误处理
- 显示网络错误提示
- 支持重试
- 离线状态提示

## 十四、多语言支持

### 14.1 登录页面文本
- 按钮文本
- 输入框提示
- 错误提示
- 加载状态提示

### 14.2 需要添加的本地化键
- `loginTitle`: "登录"
- `googleLogin`: "使用 Google 登录"
- `appleLogin`: "使用 Apple 登录"
- `emailLogin`: "邮箱登录"
- `enterEmail`: "请输入邮箱"
- `sendCode`: "发送验证码"
- `enterCode`: "请输入验证码"
- `loginButton`: "登录"
- `loginFailed`: "登录失败"
- `invalidCode`: "验证码错误"
- `codeSent`: "验证码已发送"
- `loggingIn`: "登录中..."

## 十五、安全考虑

### 15.1 Token 安全
- ✅ 使用 HTTPS 传输
- ✅ Refresh Token 存储在安全存储中
- ✅ Access Token 短期有效
- ✅ 登出时清除所有 Token

### 15.2 验证码安全
- ✅ 6位数字，5分钟有效
- ✅ 限制发送频率
- ✅ 限制尝试次数
- ✅ 使用后立即失效

### 15.3 第三方登录安全
- ✅ 后端验证第三方 Token
- ✅ 不信任前端传递的用户信息
- ✅ 验证 Token 签名和过期时间

## 十六、用户体验优化

### 16.1 登录体验
- ✅ 清晰的登录方式选择
- ✅ 友好的加载状态
- ✅ 明确的错误提示
- ✅ 支持快速重试

### 16.2 启动体验
- ✅ 启动时显示加载动画
- ✅ 快速验证登录状态
- ✅ 无缝跳转到对应页面

### 16.3 视觉设计
- ✅ 现代化的 UI 设计
- ✅ 清晰的按钮样式
- ✅ 响应式布局
- ✅ 支持深色模式
